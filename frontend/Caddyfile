{
	admin off
	auto_https off
	persist_config off
}

:8080 {
	encode zstd gzip
	root * /srv

	@backend path /health /api/* /docs /openapi.json
	handle @backend {
		reverse_proxy backend:8000
	}

	handle {
		try_files {path} /index.html
		file_server
	}
}
