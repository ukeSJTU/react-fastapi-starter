# Docker Compose uses development-safe defaults when these values are unset.
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PORT=5432
APP_PORT=8080

# Required for the production Compose stack. Use a URL-safe value, for example:
# openssl rand -hex 32
POSTGRES_PASSWORD=
